import 'package:flutter/material.dart';

class NewPaperScreen extends StatelessWidget {
  const NewPaperScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Submit Paper')),
    );
  }
}
